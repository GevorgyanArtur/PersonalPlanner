import React from "react";
import s from "./Profile.module.css"
function Profile() {
    return(
        <div className={s.time}>
            <a href = "?" className={s.style}>My profile</a>
        </div>
    );
}

export default Profile;